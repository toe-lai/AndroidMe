<resources>

    <!-- Exists for inheritance purposes -->
    <style name="AndroidMe" parent="AndroidMe.Base" />

    <style name="AndroidMe.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/theme_primary</item>
        <item name="colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="colorAccent">@color/theme_accent</item>
    </style>

    <style name="AndroidMe.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AndroidMe.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!-- Activity styles -->
    <style name="AndroidMe.Template"/>

    <!-- Button styles -->
    <style name="Widget.AndroidMe.Button.Base" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/app_white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Button</item>
    </style>

</resources>
